# Flowz v3.2 smoke checks

- JavaScript syntax checked with `node --check`.
- Runtime smoke test rendered four modes, created a persisted mission, and started a session using a minimal DOM/localStorage harness.
- Existing learning data keys remain unchanged: `flowz_duo_data`, `flowz_duo_data_backup`, and `flowz_duo_pending`.
- Final acceptance still requires iPhone Safari confirmation after deployment.
